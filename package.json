{
  "name": "naidenovart",
  "version": "1.0.0",
  "description": "NaidenovART Portfolio - Professional art showcase",
  "main": "index.html",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html *.css *.js *.png Poetry Aseam Vehicle Awards PizzaProject Socials AboutTheArtist ProjectAlfa dist/",
    "dev": "python -m http.server 8000",
    "cap:build": "npm run build && npx cap sync",
    "cap:android": "npm run cap:build && npx cap open android",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["art", "portfolio", "mobile-app", "capacitor"],
  "author": "NaidenovART",
  "license": "ISC",
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}
